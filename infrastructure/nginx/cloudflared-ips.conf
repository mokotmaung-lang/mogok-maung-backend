# Verified origins allowed to reach the admin/agent control plane THROUGH a
# Cloudflare Tunnel. Each entry marks a trusted source IP/CIDR as 1 — requests
# from these origins are only admitted when they also carry a CF-Connecting-IP
# header (i.e. the request really traversed the Tunnel).
#
# Fill with the cloudflared origin IP / internal VPC CIDR, e.g.:
#   10.30.0.0/16 1;
#   192.168.5.10 1;