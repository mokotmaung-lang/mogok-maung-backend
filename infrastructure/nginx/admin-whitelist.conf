# Office / HQ CIDRs allowed DIRECT access to the admin & agent control plane.
# Secure default = DENY ALL: geo $admin_allowed default 0 in nginx.conf rejects
# everything not listed below (equivalent to "deny all;").
# One line per CIDR, value 1 (geo module format), e.g.:
#   203.0.113.0/24 1;
#   2001:db8:123::/48 1;

# --- Master office public IP ---
203.31.93.63/32 1;

# --- Loopback / Docker bridge for local checks (health probes) ---
127.0.0.0/8 1;
172.16.0.0/12 1;